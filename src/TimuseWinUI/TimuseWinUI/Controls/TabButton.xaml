<UserControl
    x:Class="TimuseWinUI.Controls.TabButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TimuseWinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="control">

    <UserControl.Resources>
        <LinearGradientBrush x:Key="BackgroundActive">
            <GradientStop Color="#396AFC" Offset="0"/>
            <GradientStop Color="#2A4AFF" Offset="1"/>
        </LinearGradientBrush>
        <SolidColorBrush x:Key="BaseGray" Color="#7D87A5"></SolidColorBrush>
    </UserControl.Resources>
    <Grid Background="Transparent">
        <Border x:Name="border" CornerRadius="8"/>
        <StackPanel Margin="32 0 0 0" VerticalAlignment="Center" Orientation="Horizontal">
            <Viewbox Width="30" Height="30">
                <Image x:Name="icon" Source="{x:Bind InactiveIcon,Mode=OneTime}"/>
            </Viewbox>
            <TextBlock x:Name="text" Margin="16 0 0 0" Text="{x:Bind Text,Mode=OneTime}" Foreground="{StaticResource BaseGray}" VerticalAlignment="Center" FontSize="16"/>
        </StackPanel>
    </Grid>
</UserControl>
